import React from "react";
import Product from "./Product";
//create + export Price component
export function Price () {
  return <p>{Product.Price}</p>; // Render the image property of the product object
};

 